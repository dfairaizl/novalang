export function add(x: Int, y: Int) -> Int {
  const res = x + y;
  return res;
}

export function subtract(x: Int, y: Int) -> Int {
  return x - y;
}

export function multiply(x: Int, y: Int) -> Int {
  return x * y;
}

export function divide(x: Int, y: Int) -> Int {
  return x / y;
}

export function doMath() -> Int {
  return 765;
}
