function add(x: Int, y: Int) -> Int {
  const res = x + y;
  return res;
}

function subtract(x: Int, y: Int) -> Int {
  return x - y;
}

function multiply(x: Int, y: Int) -> Int {
  return x * y;
}

function divide(x: Int, y: Int) -> Int {
  return x / y;
}

export function doMath() -> Int {
  return 765;
}
