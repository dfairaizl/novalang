function checkArgumentBinding (argNode, refNode) {
  if (refNode.attributes.type === 'variable_reference') {
    if (refNode.attributes.identifier === argNode.attributes.identifier) {
      return true;
    }
  }

  return false;
}
