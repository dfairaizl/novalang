import printf from 'io';

class Calculator {
  let on = false

  isTurnedOn() -> Boolean {
    return this.on;
  }

  turnOn() {
    this.on = true;
  }
}

const c = new Calculator();

c.turnOn();
const isOn = c.isTurnedOn();

printf("Calculator is %d", isOn);
