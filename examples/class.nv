import printf from 'io';

class Calculator {
  add(x: Int, y: Int) -> Int {
    return x + y;
  }

  sub(x: Int, y: Int) -> Int {
    return x - y
  }
}

const a = new Calculator();
const b = a.add(1, 1);
const c = a.sub(1, 1);

printf("The answer b %d", b);
printf("The answer c %d", c);
